apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpiece-app
spec:
  selector:
      matchLabels:
        app: mailpiece-app
  replicas: 1
  template:
    metadata:
      labels:
        app: mailpiece-app
    spec:
      containers:
        - name: mailpiece-app
          image: mailpiece-app:0.1.0-SNAPSHOT
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: SPRING_PROFILES_ACTIVE
              value: "k8"
          ports:
            - containerPort: 9090
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
---
kind: Service
apiVersion: v1
metadata:
  name: mailpiece-app
spec:
  selector:
    app: mailpiece-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 9090
      nodePort: 30090
